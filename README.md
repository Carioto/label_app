This app was designed to run from a flash drive mapped to E:/

This app requires the WinPython mobile directory including the WinPythhon/python-3.11.9.amd64/pythonw.exe

to run use pythonw.exe launch.py 
the src/ directory contains the python files that are obfuscated with cpython when in production
